{
  "name": "amigo",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun --filter '!@amigo-llm/types' start",
    "build": "bun --filter '@amigo-llm/types' build && bun --filter '@amigo-llm/server' build && bun --filter '@amigo-llm/frontend' build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "prepare": "lefthook install",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun run build && changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^2.3",
    "@changesets/cli": "^2.29.8",
    "lefthook": "^1.9.4"
  },
  "workspaces": [
    "packages/**"
  ]
}
